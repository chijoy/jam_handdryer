require "spec_helper"

describe JamHanddryer do
  it "has a version number" do
    expect(JamHanddryer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
